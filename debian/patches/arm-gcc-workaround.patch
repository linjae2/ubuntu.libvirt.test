diff -Naurp libvirt-0.9.6.orig//configure libvirt-0.9.6//configure
--- libvirt-0.9.6.orig//configure	2011-09-22 02:49:44.000000000 -0400
+++ libvirt-0.9.6//configure	2011-10-09 22:23:00.903801420 -0400
@@ -54466,6 +54466,12 @@ $as_echo "$ac_res" >&6; }
     fi
   done
 
+# see https://bugs.launchpad.net/bugs/823711
+case "$host" in
+	arm*-*linux* )
+		LIBNL_LIBS=`echo $LIBNL_LIBS | sed 's/ *-lnl-[^ ]*//g'`
+		;;
+esac
 
   { $as_echo "$as_me:${as_lineno-$LINENO}: checking for iconv" >&5
 $as_echo_n "checking for iconv... " >&6; }
diff -Naurp libvirt-0.9.6.orig//configure.ac libvirt-0.9.6//configure.ac
--- libvirt-0.9.6.orig//configure.ac	2011-09-22 02:42:11.000000000 -0400
+++ libvirt-0.9.6//configure.ac	2011-10-09 22:23:44.403801425 -0400
@@ -2427,8 +2427,14 @@ fi
 AM_CONDITIONAL([WITH_VIRTUALPORT], [test "$with_virtualport" = "yes"])
 
 
-dnl netlink library
 
+dnl netlink library
+# see https://bugs.launchpad.net/bugs/823711
+case "$host" in
+	arm*-*linux* )
+		LIBNL_LIBS=`echo $LIBNL_LIBS | sed 's/ *-lnl-@<:@^ @:>@*//g'`
+	;;
+esac
 LIBNL_CFLAGS=""
 LIBNL_LIBS=""
 have_libnl=no
