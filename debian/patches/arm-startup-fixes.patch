diff -Naurp libvirt-0.9.2.orig//src/node_device/node_device_udev.c libvirt-0.9.2/src/node_device/node_device_udev.c
--- libvirt-0.9.2.orig//src/node_device/node_device_udev.c	2011-09-09 13:02:53.000000000 -0500
+++ libvirt-0.9.2/src/node_device/node_device_udev.c	2011-09-09 13:19:27.000000000 -0500
@@ -1423,7 +1423,7 @@ static int udevDeviceMonitorShutdown(voi
         ret = -1;
     }
 
-#if defined __s390__ || defined __s390x_
+#if defined __s390__ || defined __s390x_ || defined __arm__
     /* Nothing was initialized, nothing needs to be cleaned up */
 #else
     /* pci_system_cleanup returns void */
@@ -1477,7 +1477,8 @@ out:
     return;
 }
 
-
+/* DMI is intel-compatible specific */
+#if defined(__x86_64__) || defined(__i386__) || defined(__amd64__)
 static void
 udevGetDMIData(union _virNodeDevCapData *data)
 {
@@ -1550,6 +1551,7 @@ out:
     }
     return;
 }
+#endif
 
 
 static int udevSetupSystemDev(void)
@@ -1574,7 +1576,9 @@ static int udevSetupSystemDev(void)
         goto out;
     }
 
+#if defined(__x86_64__) || defined(__i386__) || defined(__amd64__)
     udevGetDMIData(&def->caps->data);
+#endif
 
     dev = virNodeDeviceAssignDef(&driverState->devs, def);
     if (dev == NULL) {
@@ -1600,7 +1604,7 @@ static int udevDeviceMonitorStartup(int
     struct udev *udev = NULL;
     int ret = 0;
 
-#if defined __s390__ || defined __s390x_
+#if defined __s390__ || defined __s390x_ || defined __arm__
     /* On s390(x) system there is no PCI bus.
      * Therefore there is nothing to initialize here. */
 #else
diff -Naurp libvirt-0.9.2.orig//src/util/sysinfo.c libvirt-0.9.2/src/util/sysinfo.c
--- libvirt-0.9.2.orig//src/util/sysinfo.c	2011-09-09 13:02:53.000000000 -0500
+++ libvirt-0.9.2/src/util/sysinfo.c	2011-09-09 13:21:41.000000000 -0500
@@ -82,7 +82,10 @@ void virSysinfoDefFree(virSysinfoDefPtr
  *
  * Returns: a filled up sysinfo structure or NULL in case of error
  */
-#ifdef WIN32
+#if defined(WIN32) || \
+    !(defined(__x86_64__) || \
+      defined(__i386__) ||   \
+      defined(__amd64__))
 virSysinfoDefPtr
 virSysinfoRead(void) {
     /*
@@ -94,7 +97,7 @@ virSysinfoRead(void) {
     return NULL;
 }
 
-#else /* !WIN32 */
+#else /* !WIN32 && !X86 */
 
 virSysinfoDefPtr
 virSysinfoRead(void) {
@@ -211,7 +214,7 @@ no_memory:
     ret = NULL;
     goto cleanup;
 }
-#endif /* !WIN32 */
+#endif /* !WIN32 && !X86 */
 
 /**
  * virSysinfoFormat:
