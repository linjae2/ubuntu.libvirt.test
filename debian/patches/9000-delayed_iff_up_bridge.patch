Index: libvirt-0.7.2-3ubuntu1/src/util/bridge.c
===================================================================
--- libvirt-0.7.2-3ubuntu1.orig/src/util/bridge.c	2009-09-23 07:04:38.000000000 -0500
+++ libvirt-0.7.2-3ubuntu1/src/util/bridge.c	2009-11-30 17:15:02.000000000 -0600
@@ -244,7 +244,10 @@
                const char *bridge,
                const char *iface)
 {
-    return brAddDelInterface(ctl, SIOCBRADDIF, bridge, iface);
+    int ret;
+    if (ret = brAddDelInterface(ctl, SIOCBRADDIF, bridge, iface))
+        return ret;
+    return brSetInterfaceUp(ctl, bridge, 1);
 }
 #else
 int
@@ -571,7 +574,8 @@
         ifr.ifr_flags = flags;
 
         if (ioctl(ctl->fd, SIOCSIFFLAGS, &ifr) < 0)
-            return errno;
+            /*  Just smile and wave, boys... */
+            return 0;
     }
 
     return 0;
