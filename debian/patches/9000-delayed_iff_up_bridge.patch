diff -Nur libvirt-0.7.5/src/util/bridge.c libvirt-0.7.5.new/src/util/bridge.c
--- libvirt-0.7.5/src/util/bridge.c	2009-12-22 03:37:57.000000000 -0600
+++ libvirt-0.7.5.new/src/util/bridge.c	2010-01-22 14:43:32.899529653 -0600
@@ -244,7 +244,10 @@
                const char *bridge,
                const char *iface)
 {
-    return brAddDelInterface(ctl, SIOCBRADDIF, bridge, iface);
+    int ret;
+    if (ret = brAddDelInterface(ctl, SIOCBRADDIF, bridge, iface))
+        return ret;
+    return brSetInterfaceUp(ctl, bridge, 1);
 }
 #else
 int
@@ -571,7 +574,8 @@
         ifr.ifr_flags = flags;
 
         if (ioctl(ctl->fd, SIOCSIFFLAGS, &ifr) < 0)
-            return errno;
+            /*  Just smile and wave, boys... */
+            return 0;
     }
 
     return 0;
